[project]
requires-python = ">=3.12"
name = "p2p-hub-api"
version = "0.1.0"
dependencies = [
    "fastapi>=0.139.2",
    "httpx>=0.28.1",
    "psycopg>=3.3.4",
    "sqlmodel>=0.0.39",
]

[tool.fastapi]
entrypoint = "main:app"
