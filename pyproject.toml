[project]
requires-python = ">=3.12"
name = "p2p-hub-api"
version = "0.1.0"
dependencies = [
    "fastapi>=0.139.2",
    "gunicorn>=26.0.0",
    "httpx>=0.28.1",
    "python-dotenv>=1.2.2",
    "sqlmodel>=0.0.39",
    "uvicorn>=0.51.0",
]
[tool.fastapi]
entrypoint = "app.main:app"
